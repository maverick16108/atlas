# Workflow: Debug

1.  **Replicate**: Create a test case (or manual step) that reproduces the issue.
2.  **Analyze**: Check logs in `storage/logs/laravel.log`.
3.  **Hypothesize**: Form a theory on the root cause.
4.  **Fix**: Apply the fix.
5.  **Verify**: Run the reproduction test case to ensure it passes.
